#pragma once
#include "LSBuffer.h"
#include <d3d11.h>
// A suite of functions to help with LS conversion - specifically templated ones to prevent breaking intellisense for now
namespace LS
{
    /*template<class TObject>
    inline D3D11_BUFFER_DESC CreateBufferDesc(const LS::LSBuffer<TObject>& buffer)
    {
        D3D11_BUFFER_DESC out;
        out.ByteWidth = buffer.GetSizeInBytes();
        out.Usage = 
    }*/
}